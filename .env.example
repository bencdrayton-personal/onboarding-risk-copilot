# Copy to .env and fill in. Both are optional — the repo runs without either.

# For the live Claude backend (python cli.py --model anthropic):
ANTHROPIC_API_KEY=

# For live ABN Lookup enrichment (free GUID from abr.business.gov.au/Tools/WebServices):
ABN_LOOKUP_GUID=
